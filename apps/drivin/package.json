{
  "name": "@apex/drivin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5181 --strictPort --host",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@apex/engine": "*",
    "postprocessing": "^6.39.4",
    "three": "^0.185.1"
  }
}
