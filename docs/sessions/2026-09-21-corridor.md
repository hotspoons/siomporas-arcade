# Corridor POC — running checklist

*Kept current by Claude during the session. Status words: **done** (verified with a repro/probe),
**partial** (in, but not to the standard you asked for), **open** (not started or not fixed),
**blocked** (needs something from you). Newest asks at the top of each section. If something is
missing here, it was dropped — say so.*

Last updated: 2026-09-21 ~02:55 UTC — **cadre mode**: feature work is split across three spawned agents plus the running editor agent, see `2026-09-21-cadre.md`; main integrates.

## Viewer / rendering bugs you reported

| ask | status | where it stands |
|---|---|---|
| Car shakes violently (dive/squat) while driving | **done** | Root cause: the strip's pavement height came from the nearest 5 m station (stepped) and its lookup was nearest-vertex; the car followed that, not the rendered spline road. Now one height function: the carriageway spline. Repro probe: ground within 2.3 cm of the road, only the road's own grade remains. |
| Grass verge floating in the air beside a bridge deck (Braddock, stance sent) | **→ world-look agent** | Likely the strip's 40 m verge blending toward a valley floor it cannot reach; fix is no verge inside bridge intervals. |
| "Ribbons" on steep embankments beside the road | **open — need a stance** | Not reproduced yet. Very likely the same class as the horizon: the coarse terrain mesh poking through the fine strip on steep sides (`sinkUnderStrip` lowers it 2.5 m, which is not enough on a 40° cut). Design fix would be to cut terrain triangles out of the strip footprint as the horizon now is. Paste a stance (C) and I will reproduce first. |
| Green carpet on/over the road, every site, default load | **done** | Root cause (found by you): the 60 m horizon mesh under the near terrain poking through. Horizon triangles inside the near-DEM footprint are removed, rim stitched under. Repro: Sideling and Braddock default loads clean. |
| Grass: hiccup every ~0.75 s while driving; can't keep up with the car; swaying in the distance; April not September; sprites untunable; no regional types | **redesigned, → grass agent** | Cause of the hiccup: the whole ring (up to 400k blades) was regenerated and re-uploaded every 5 m. Now cached 8 m tiles, blades near / sprite cards far, cards thinned toward a 300 m+ rim, wind fades out above 4 m/s, colour/dry/sprite knobs, re-plan on teleport. Your grass JSON is the default. Not yet measured (probe kept dying on HMR reloads) — the grass agent's first task. |
| Grass "kinda lame" / too thin | **superseded** | Bezier blades, three wind layers, AO/backlight, skill densities (40/m² mown, 22/m² rough), grass ground texture under them. From the seat it still reads thin at the shoulder; the blade width and near density need another pass and probably a clump texture card for the far ring. |
| Texture repetition on the road | **done** | Hex tiling over 3 flux variants per class + 8 m macro map. |
| Angular lane paint / camera snapping at OSM nodes | **done** | Gaussian-smoothed spine before splining; one Catmull-Rom drives road, paint, grading, car. |
| Beige/teal far terrain | **done** | Horizon draped with 60 m NAIP, saturated; fog thinned. |
| Trees growing out of the freeway | **done** | CHM zeroed over every carriageway at export + 3 m exclusion from any pavement edge in the viewer. |
| Layers of grass on top of each other | **done** | Verge ribbons replaced by the single strip surface. |
| Bridge parapets only eastbound | **done** | Siblings share the spine grade and get parapets. |
| Pink/green analysis markers everywhere | **done** | Markers/spine off by default, hidden in drive mode. |
| Mobile: no way to move about | **done** | Move pad (hold to glide) + drive pad. |
| Mobile: bursts and one-second freezes | **done** | Impostor slots static with partial uploads; grass placement hashed. Not re-verified on your phone since — tell me. |
| Low-poly tree stand-ins not great | **done** | ez-tree near + impostors far, seasonal. |

## Features you asked for

| ask | status | where it stands |
|---|---|---|
| Horse-bridge prop over the road + flatten the lidar noise under it as a flat underpass, from the editor + asset library (Bowie, Race Track Rd) | **done** — bridge was 90° off (stray +π/2 in `buildBridges`), fixed; editor mode `4 · structures` shipped by the editor agent; verified at your stance | Root cause of the mess there: the 2014 Sandy NCR lidar uses class 17/18 as junk bins (23% each); demoted now, and a bridge must stand above ground to count — all nine re-profiling. Editor agent (round 2, spawned) is building `structures.json` (flatten / suppress / bridge_over on a spine interval), the editor mode, and a generated covered horse-bridge asset with `fit: "span"`. Viewer side is in (`src/structures.ts`: flatten rewrites the grade before the spline, suppress drops detections, bridge_over places the asset or a slab with abutments); verifying with a temporary override. |
| One-way carriageways: lane band 0.9 m right of the OSM centreline (asphalt symmetric about the spine) | **→ road-and-car agent** | Found by their geometry probe; fix is asymmetric asphalt behind a knob. |
| `sideling-i68` is actually US 40 Scenic over Sideling Hill (2-lane, 55 m hairpin) | **noted** | Bake is right, slug is wrong; rename + a real I-68 site later. |
| Lanes ~35% too narrow on a 2-lane road (Bowie) | **done (verified)** | `roadMesh` computed the asphalt width without the two-way flag: one 1.2 m inside shoulder instead of two 3.0 m outside ones, so the edge line sat 2.76 m from centre where 3.66 was meant (0.75×). Fixed; road-and-car's geometry probe reads 6.66 m paved half and edge lines at ±3.66 m on Bowie. |
| Centre line matches passing zones; turn lanes, forks, merges painted from OSM | **→ road-surface agent** | Depends on what OSM carries here: `overtaking=*`, `turn:lanes`, `lanes:forward/backward`. Survey below in this doc. |
| Two-way two-lane roads rendered as such (double yellow centre, white edges both sides), from OSM `oneway` | **done — verify on Bowie** | `oneway=no`, or untagged non-motorway → double yellow centre, white both edges, lanes split per side; paved width uses two outside shoulders. |
| Inset map: expand to full screen and shrink back | **done** | ⤢ button or N; scale kept so expanding shows more ground. |
| Inset top-down map with OSM layer while driving | **done** | North-up canvas: NAIP under, OSM roads by class fading with zoom, our carriageways, heading chevron, scale bar; wheel zooms about the cursor, drag pans, ⌖ re-follows. |
| Configuration editor with tabs/sub-tabs (grass density + LOD cutoffs, tree LODs, LOD shape semicircle vs circle) | **partial → editor-knobs agent** | `src/tuning.ts` + engine TunePanel, F6, tabs grass/trees/LOD shape/road/car/camera; changes re-seed live; LOD footprint stretched behind the view, circular when pitched down (`lodDistance`). Not yet verified visually. |
| Tree pop-in from sprites | **→ editor-knobs agent** | Known: near/impostor switch is a hard boundary at TREE_NEAR_RADIUS; needs a crossfade band. Knobs now expose the radius. |
| Stance exporter (reproduce my exact view) | **done** | C copies a URL with site/season/mode/camera-or-car/layers/panel; `probes/corridor-stance.mjs` renders it. The round-trip probe failure was the probe (same-URL goto = no reload), not the feature. |
| Hide the side menu | **done** | M key or the × tab; hidden state travels in the stance. |
| Controls: fly like trailworks | **done** | WASD/QE/RF/TG, right-drag look, wheel dolly. |
| Drive like drivin'/stuntin' — full port; slide steering inverted; ½-mile turning radius; off-road grip too low; launches too easy | **ported, → car agent** | Faithful stuntin port landed (`car.ts`, handling probe). Launch now needs ≥ 20 m/s and a 0.9 m gap (Bacon Ridge yes, a county crest no); grass grip/steer/traction raised to 0.7/0.9/0.85 — set blind, car agent measures. |
| Cockpit view (C while driving; stance copy moved to X) | **done, unverified** | eye/forward/look-up knobs in camera tab. |
| Fly camera can't get low | **done** | `CAM_MIN_HEIGHT` 0.4 m (was a fixed 1.6). |
| Arrow keys move the tuner while driving | **done** | keydown blurs a focused slider/select while driving and eats the arrows. |
| Geology: shorter summary + full-text dialog | **done** | names on one line, `full text` button opens a dialog. |
| Tuner panel chopped / scrolls sideways | **done (CSS), verify** | `#tunehost` width capped, no horizontal scroll. |
| Season in the tuner; winter bare trees; rain/sleet/snow/ice | **→ weather-seasons agent** | |
| Contextual base ground (turf/forest floor/rock/sand/clay/crops), displacement; landscape floats above imagery | **→ ground agent** | |
| Pavement blend band (≥ 6 in, configurable) | **→ road-surface agent** | |
| Canyons/cuts with rock walls; exposed rock (Bonnie Branch); water | **→ terrain-features agent** | |
| Gaussworks backroads + coastal sets baked | **running** | 16 sites queued from the trailworks brief (Patuxent River, Bacon Ridge, Chesterfield, Crownsville, Underwood, Waterbury, Rutland, Rossback, Hawkins, Bell Branch, St Stephens Church), Bonnie Branch, Ragged Point, Bixby, Ecola, Acadia. Log in scratchpad `newbakes.log`. |
| Design/pipeline docs | **→ docs-and-sites agent** | |
| Tree collision | **done** | Trunk circles, push-out + bounce. |
| Seasons for foliage and grass | **done** | winter/spring/summer/autumn palette; trees, grass, ground tint, sky. |
| Procedural trees at lower detail | **done** | Impostor atlas of the same ez-tree variants, top-down cell for steep views. |
| Adjustment areas + editor | **done** (editor agent) | `editor.html`; 9 sites seeded; viewer consumes all keys incl. ground offset. |
| Placement editor + catalog | **done** (editor agent) | 10 catalog entries, 3 real GLBs (barn, diner, water tower); viewer renders them. |
| Autogen design (neighbourhoods, business parks) | **design only** | `tools/corridor/AUTOGEN.md`; manifest carries buildings/landuse/POIs per its spec. No generator yet. |
| Corridor tool Dockerfile + Helm + PVC + S3 publish | **done, not deployed** | Chart lints; publish dry-run OK; image workflow ran on push. Not yet run on gh200-1. |
| Move build/publish to kube, R2 storage, Worker reads bucket | **open** | Publish command and `?data=` viewer param exist; no bucket, no Worker changes. |
| Horizon vibes: big hills / mountains filling the windscreen | **partial** | 60 km horizon in; Catoctin visible from Braddock. Reads flat-lit; needs atmosphere/haze and a better far material. |
| Rock cuts / boulders per geology (Sideling shale etc.) | **open** | Geology per station is in the data; the flux→TRELLIS boulder chain is proven; no cut-face props placed yet. |
| Real car model | **open** | Red box. |
| Recon (TRELLIS.2) on the new cluster | **done** | Verified end to end, public URL. |
| flux.2 in the new cluster | **done** (you) | Edits work on the pod; 404 through the platform gateway for multipart (platform routing). |
| Use pai-managed-ext-gateway next time | **noted** | In memory + values.yaml. |
| Bake all 9 sites | **done; re-profiled** | Bowie's 46 fake overpasses: a deck is a plane — underside std ≤ 0.5 m / range ≤ 1.5 m across the road (real decks measure ≤ 0.28 / 0.74; Race Track Road's canopy 1.2–5.3 / 3–15). Bowie now 3 structures incl. the OSM bridleway at s≈2350. All nine re-profiled; CHM zeroed under building footprints and re-exported. |
| Commit and push | **done** | `4170588` on main. Generated textures/GLBs gitignored (regenerable, ~100 MB). |

## OSM lane-marking data actually present in the nine corridors (surveyed 2026-09-21)

| tag | on our spines | meaning for paint |
|---|---|---|
| `lanes` | 100 segments | lane count (used) |
| `oneway` | 92 | carriageway vs two-way (used) |
| `turn:lanes` | 27 spine segments, 129 features overall | per-lane arrows and `merge_to_left/right` — enough to paint merge tapers and turn arrows on the interstates |
| `destination:lanes` | 12 | sign text per lane (later: gantry signs) |
| `lanes:forward` / `lanes:backward` | 1 each | asymmetric two-way (a climbing lane) |
| `overtaking=*` | **0** | no passing-zone data anywhere here — a dashed vs solid centre line cannot come from OSM on these roads; it would need the editor (an authored interval) or a sight-distance rule from the profile |

## Process

| ask | status |
|---|---|
| Repro + root cause before any fix; isolate by hiding layers | **adopted** — saved to memory; this doc records the cause for each fix |
| Keep a running checklist you can see | **this file** |
| Sub-agent via mailbox | **done** — `/tmp/corridor-mail/`, editor agent finished and signed off |

## Next on my side, in order
1. Stance URL reload (so you can send me the ribbons).
2. Ribbons: reproduce from your stance, then cut the terrain out of the strip footprint if that is it.
3. Grass density/width pass from the seat.
4. Car model; rock cut props; horizon atmosphere.
