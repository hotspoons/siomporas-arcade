apiVersion: v2
name: assetsvc
description: The backend that orchestrates AI asset generation for corridor — the only thing that calls a GPU service
type: application
version: 0.1.0
appVersion: "0.1.0"
