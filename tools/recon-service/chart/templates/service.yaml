apiVersion: v1
kind: Service
metadata:
  name: {{ include "recon.fullname" . }}
  labels: {{- include "recon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "recon.selectorLabels" . | nindent 4 }}
